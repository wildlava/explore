To create the needed files for building with ant, do:

    cd tools; ./mkicons; cd ..
    android update project --name Explore --target 4 -p .

  if needed:
    cat ~/android/keys/explore_key_settings >> local.properties
